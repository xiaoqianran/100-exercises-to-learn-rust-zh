// 这是一个 Rust 源文件：纯文本，扩展名为 `.rs`。
//
// 和大多数现代语言一样，Rust 支持注释。你现在看到的就是注释！
// 注释会被编译器忽略，用来给代码写说明。
// 注释有多种写法；这里先用最常见的「行注释」：
// 从 `//` 开始到行尾都是注释。

// 练习里会出现 `TODO`、`todo!()` 或 `__` 等标记，提醒你要改哪里。
// 你需要把这些标记替换成自己的代码才能完成本题。
// 有时改一行就够，有时要写更长的一段。
//
// 若某一题卡住超过 10 分钟，可以对照上游仓库的 `solutions` 分支，
// 或先重读本目录的 `教学.md`。
fn greeting() -> &'static str {
    // TODO: 修复下面这一行 👇
    "I'm ready to __!"
}

// 你的实现会由一组测试自动校验。
// 可在本练习目录（或仓库根用 `-p`）运行 `cargo test`。
//
// Rust 允许把测试和业务代码写在同一文件。
// `#[cfg(test)]` 表示下面模块只在测试时编译。
//
// ⚠️ **不要修改测试** ⚠️
// 只改被测代码，不要改测试本身。
#[cfg(test)]
mod tests {
    use crate::greeting;

    #[test]
    fn test_welcome() {
        assert_eq!(greeting(), "I'm ready to learn Rust!");
    }
}
