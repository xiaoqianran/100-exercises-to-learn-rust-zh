# 工单 v2 / 带数据的变体

> 状态：骨架（后续完善） · 上游练习：`05_ticket_v2/03_variants_with_data` · crate：`variants_with_data`

## 1. 本题目标

完成本题 `03_variants_with_data`，理解「带数据的变体」相关概念，并通过全部单元测试。

章节主题：枚举、错误处理、依赖与 thiserror

## 2. 你需要改哪里

- 主文件：[`src/lib.rs`](./src/lib.rs)
- 包清单：[`Cargo.toml`](./Cargo.toml)
- 阅读 `src/lib.rs` 中的 TODO / `todo!()` / `__` 标记并补全实现；**不要修改测试**。

⚠️ **不要修改测试模块**（`#[cfg(test)]` 下的代码），除非题目明确要求。

## 3. 如何运行

在**仓库根目录**执行（推荐）：

```bash
cargo test -p variants_with_data
```

或在本练习目录执行：

```bash
cd exercises/05_ticket_v2/03_variants_with_data
cargo test
```

只看失败测试：

```bash
cargo test -p variants_with_data -- --nocapture
```

生成文档（部分题有文档注释时有用）：

```bash
cargo doc -p variants_with_data --open
```

## 4. 期望结果 / 通过标准

`cargo test` 全部通过（全部 `ok`）。

终端中应看到类似：

```text
test result: ok. X passed; 0 failed; ...
```

## 5. 思路提示

- 先阅读教程：`book/src/05_ticket_v2/03_variants_with_data.md`（若存在）。
- 认真阅读编译器与测试失败信息。
- 卡住超过 10 分钟可对照上游 `solutions` 分支思路（本仓库鼓励先独立完成）。

英文原书摘录（翻译完善中）：

```
# Variants can hold data
```rust
enum Status {
ToDo,
InProgress,
Done,
}
```
```

## 6. 常见错误

- 修改了测试代码
- 跳过编译错误直接猜答案
- 在错误的目录运行 cargo

## 7. 相关教程

- 本书对应章节：`book/src/05_ticket_v2/03_variants_with_data.md` ✅
- 课程总目录：[`book/src/SUMMARY.md`](../../../book/src/SUMMARY.md)
- 中文总说明：[`README.md`](../../../README.md)

## 8. 完成后

- [ ] `cargo test -p variants_with_data` 全部通过
- [ ] 用自己的话在笔记里写一句「本题我学到了……」
- [ ] 进入下一题目录继续

---

本中文延伸内容基于 [mainmatter/100-exercises-to-learn-rust](https://github.com/mainmatter/100-exercises-to-learn-rust)（CC BY-NC 4.0）整理。
