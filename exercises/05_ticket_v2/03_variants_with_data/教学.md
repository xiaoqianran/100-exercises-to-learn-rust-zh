# 工单 v2 / 带数据的变体

> 状态：完整 · 上游：`05_ticket_v2/03_variants_with_data` · crate：`variants_with_data`

## 1. 本题目标

从 `InProgress { assigned_to }` 取出负责人。

## 2. 你需要改哪里

- 主文件见练习目录 `src/`
- match status；非 InProgress 则 panic 指定消息。

## 3. 如何运行

```bash
cargo test -p variants_with_data
```

```bash
cd exercises/05_ticket_v2/03_variants_with_data && cargo test
```

binary 练习可用：

```bash
cargo run -p variants_with_data
```

## 4. 期望结果

Alice 用例与两个 should_panic 通过。

## 5. 思路提示

- if let Status::InProgress { assigned_to } = &self.status { assigned_to } else { panic!(...) }
- 消息：`Only \`In-Progress\` tickets can be assigned to someone`



## 6. 常见错误

- 消息文案不一致。

## 7. 相关教程

- `book/src/05_ticket_v2/03_variants_with_data.md`

## 8. 完成后

- [ ] 测试通过
- [ ] 进入下一题

---
基于 mainmatter/100-exercises-to-learn-rust（CC BY-NC 4.0）。
