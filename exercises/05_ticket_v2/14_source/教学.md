# 工单 v2 / Error::source

> 状态：完整 · 上游：`05_ticket_v2/14_source` · crate：`source`

## 1. 本题目标

错误链：无效 status 时 source 指向 ParseStatusError。

## 2. 你需要改哪里

- 主文件见练习目录 `src/`
- TicketNewError 增加变体并 #[source]；new 解析 status 字符串。

## 3. 如何运行

```bash
cargo test -p source
```

```bash
cd exercises/05_ticket_v2/14_source && cargo test
```

binary 练习可用：

```bash
cargo run -p source
```

## 4. 期望结果

invalid status 测试：to_string 与 source().is_some()。

## 5. 思路提示

- #[error(transparent)] 或 #[error("...", source)]
- thiserror: #[source] / #[from] ParseStatusError
- Status::try_from(status).map_err(...)?



## 6. 常见错误

- source 为 None；错误 Display 不对。

## 7. 相关教程

- `book/src/05_ticket_v2/14_source.md`

## 8. 完成后

- [ ] 测试通过
- [ ] 进入下一题

---
基于 mainmatter/100-exercises-to-learn-rust（CC BY-NC 4.0）。
