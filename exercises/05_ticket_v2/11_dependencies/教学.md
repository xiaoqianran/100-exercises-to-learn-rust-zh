# 工单 v2 / 依赖

> 状态：完整 · 上游：`05_ticket_v2/11_dependencies` · crate：`deps`

## 1. 本题目标

在 Cargo.toml 添加第三方依赖。

## 2. 你需要改哪里

- 主文件见练习目录 `src/`
- 添加 anyhow，使 `use anyhow::Error` 通过。

## 3. 如何运行

```bash
cargo test -p deps
```

```bash
cd exercises/05_ticket_v2/11_dependencies && cargo test
```

binary 练习可用：

```bash
cargo run -p deps
```

## 4. 期望结果

cargo check -p deps 通过。

## 5. 思路提示

- [dependencies] anyhow = "1"



## 6. 常见错误

- 写错 crate 名；加到 dev-dependencies 导致 lib 用不了。

## 7. 相关教程

- `book/src/05_ticket_v2/11_dependencies.md`

## 8. 完成后

- [ ] 测试通过
- [ ] 进入下一题

---
基于 mainmatter/100-exercises-to-learn-rust（CC BY-NC 4.0）。
