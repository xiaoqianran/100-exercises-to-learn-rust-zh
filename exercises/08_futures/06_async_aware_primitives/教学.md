# 异步 Futures / 异步原语

> 状态：骨架（后续完善） · 上游练习：`08_futures/06_async_aware_primitives` · crate：`async_locks`

## 1. 本题目标

完成本题 `06_async_aware_primitives`，理解「异步原语」相关概念，并通过全部单元测试。

章节主题：async/await、运行时与取消

## 2. 你需要改哪里

- 主文件：[`src/lib.rs`](./src/lib.rs)
- 包清单：[`Cargo.toml`](./Cargo.toml)
- 阅读 `src/lib.rs` 中的 TODO / `todo!()` / `__` 标记并补全实现；**不要修改测试**。

⚠️ **不要修改测试模块**（`#[cfg(test)]` 下的代码），除非题目明确要求。

## 3. 如何运行

在**仓库根目录**执行（推荐）：

```bash
cargo test -p async_locks
```

或在本练习目录执行：

```bash
cd exercises/08_futures/06_async_aware_primitives
cargo test
```

只看失败测试：

```bash
cargo test -p async_locks -- --nocapture
```

生成文档（部分题有文档注释时有用）：

```bash
cargo doc -p async_locks --open
```

## 4. 期望结果 / 通过标准

`cargo test` 全部通过（全部 `ok`）。

终端中应看到类似：

```text
test result: ok. X passed; 0 failed; ...
```

## 5. 思路提示

- 先阅读教程：`book/src/08_futures/06_async_aware_primitives.md`（若存在）。
- 认真阅读编译器与测试失败信息。
- 卡住超过 10 分钟可对照上游 `solutions` 分支思路（本仓库鼓励先独立完成）。

英文原书摘录（翻译完善中）：

```
# Async-aware primitives
If you browse `tokio`'s documentation, you'll notice that it provides a lot of types
that "mirror" the ones in the standard library, but with an asynchronous twist:
locks, channels, timers, and more.
When working in an asynchronous context, you should prefer these asynchronous alternatives
to their synchronous counterparts.
To understand why, let's take a look at `Mutex`, the mutually exclusive lock we explored
in the previous chapter.
```

## 6. 常见错误

- 修改了测试代码
- 跳过编译错误直接猜答案
- 在错误的目录运行 cargo

## 7. 相关教程

- 本书对应章节：`book/src/08_futures/06_async_aware_primitives.md` ✅
- 课程总目录：[`book/src/SUMMARY.md`](../../../book/src/SUMMARY.md)
- 中文总说明：[`README.md`](../../../README.md)

## 8. 完成后

- [ ] `cargo test -p async_locks` 全部通过
- [ ] 用自己的话在笔记里写一句「本题我学到了……」
- [ ] 进入下一题目录继续

---

本中文延伸内容基于 [mainmatter/100-exercises-to-learn-rust](https://github.com/mainmatter/100-exercises-to-learn-rust)（CC BY-NC 4.0）整理。
