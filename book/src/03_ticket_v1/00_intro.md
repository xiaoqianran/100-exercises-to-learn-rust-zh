# 工单 v1

本章用「软件工单 Ticket」建模，系统学习：

- 结构体 `struct` 与方法
- 校验与 `panic`
- 模块 `mod` 与可见性 `pub`
- 封装与访问器
- 所有权、借用与可变借用
- 栈 / 堆 / 引用的内存布局
- 作用域与析构（drop）

先完成导读：

```bash
cargo test -p intro_02
```

阅读：`exercises/03_ticket_v1/00_intro/教学.md`。
