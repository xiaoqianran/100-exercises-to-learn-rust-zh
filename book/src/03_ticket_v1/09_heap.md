# 堆

动态大小数据（如字符串内容、集合）放在堆上。  
分配器预留内存并返回指针；需要时再释放。

## `String` 的布局

栈上保存三元组：**指针 / 长度 / 容量**（各一个 `usize`）。  
64 位平台上 `size_of::<String>() == 24`。字符数据在堆上。

`Ticket` 含 3 个 `String` 时，栈大小直觉上为 `3 * 24 = 72`（本题正确；一般结构体内存布局可能有对齐填充）。

```bash
cargo test -p heap
```
