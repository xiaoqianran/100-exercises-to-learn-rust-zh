# 栈

栈是 LIFO：函数调用压栈帧，返回弹栈。分配/释放极快。

大小在编译期已知的值（如 `u32`、`bool`）常放在栈上。

用 `std::mem::size_of::<T>()` 查看类型占用的字节数：

| 类型 | 大小（字节） |
| --- | --- |
| `u16` | 2 |
| `i32` | 4 |
| `bool` | 1 |

```bash
cargo test -p stack
```
